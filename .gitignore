# Node (commitlint/husky tooling)
node_modules/

# Godot
.godot/
*.import
# .uid sidecars stay untracked (#315). Godot regenerates them on import, so
# committing them is pure churn — but that is only safe because nothing here
# resolves a resource *through* a uid. Every ext_resource in game/scenes/ and
# every autoload in project.godot references `path="res://..."`; the only
# uid:// tokens outside game/addons/ are the six [gd_scene uid="..."] identity
# fields a .tscn stores inline for itself, which have no relation to a script's
# .uid sidecar. 24 sidecars predating this rule were untracked to match it,
# rather than the reverse — committing one per script would have meant ~169
# more files that churn on every script added, for zero referential benefit.
# If a future .tscn is ever authored with ext_resource uid="uid://...", this
# decision has to be revisited before that scene is committed.
*.uid
export_presets.cfg
*.translation
game/export/

# OS
.DS_Store

# IDE
.idea/
*.swp
*.swo

# Secrets
.env

# Husky internals (regenerated by pnpm install)
.husky/_/

# Coverage / build artifacts
coverage/

# Reference materials (scraped third-party content, local-only)
docs/references/bestiary/
docs/references/menus/

# Superpowers brainstorm sessions
.superpowers/
__pycache__/
tools/__pycache__/

# beads (bd) removed — keep any local re-creation out of git
.beads/
